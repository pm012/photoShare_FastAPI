# Copy this file to frontend/.env for local development.
# Vite exposes only variables prefixed with VITE_ to browser code.
# This value is public and must not contain secrets.
VITE_API_URL=http://localhost:8000/api
